
  <input type="hidden" id="usernameId" name="usernameId" value={{usernameId}}>
  <input type="hidden" id="roomId" name="roomId" value={{room}}>
  
  <input type="hidden" id="quizName" name="quizName" value={{quizName}}>
  <input type="hidden" id="seconds" name="seconds" value={{time.seconds}}>
  <input type="hidden" id="minutes" name="minutes" value={{time.minutes}}>
  <input type="hidden" id="hours" name="hours" value={{time.hours}}>


    <div class="chat-container">
      <header class="chat-header">
        <h1><i class="fas fa-smile"></i> Quiz Lab</h1>
        <a id="leave-btn" class="btn">Leave Room</a>
      </header>
      <main class="chat-main">
        <div class="chat-sidebar">
          <h3><i class="fas fa-comments"></i> Quiz Name:</h3>
          <h2 id="room-name">{{quizName}}</h2>
          <h3><i class="fas fa-users"></i> Users</h3>
          <ul id="users"></ul>
        </div>
        <div class="chat-messages">
           <form id="quizForm">
     

            
            </form>
            <button type="button" class="btn " id="anwser-btn" ><i class="fas fa-paper-plane"></i> Anwser</button>


        </div>
      </main>
      <div class="chat-form-container">
        
        {{!-- <form id="chat-form">
          <button class="btn anwser-btn"><i class="fas fa-paper-plane"></i> Send</button>
        </form> --}}
        
      </div>
    </div>
    
    <button class="btn" id="submit-btn" ><i class="fas fa-paper-plane"></i>Submit/Finish</button>
    {{#if isAdmin}}
    <button class="btn" style="margin-left: 700px;" id="startQuiz"><i class="fas fa-paper-plane"></i>START QUIZ</button>
    {{/if}}

<div style="margin-left: 300px;">

<section class="countdown-container">
  
 
  <div class="hours-container">
    <div class="hours"></div>
    <div class="hours-label">hours</div>
  </div>
  
  <div class="minutes-container">
    <div class="minutes"></div>
    <div class="minutes-label">minutes</div>
  </div>
  
  <div class="seconds-container">
    <div class="seconds"></div>
    <div class="seconds-label">seconds</div>
  </div>
  
</section>

    {{!-- {{/if}} --}}
     
    <script src="https://cdn.socket.io/4.1.1/socket.io.min.js" integrity="sha384-cdrFIqe3RasCMNE0jeFG9xJHog/tgOVC1E9Lzve8LQN1g5WUHo0Kvk1mawWjxX7a" crossorigin="anonymous"></script>
    <!-- <script src="/socket.io/socket.io.js"></script> -->
    <script src="/js/main.js"></script>
